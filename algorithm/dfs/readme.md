<!-- 深度优先搜索 -->
Depth-First Search (DFS)
沿着一条分支一路遍历到底，回溯后再探索其余分支的图/树遍历算法
   A
  B  C
D

先序遍历为例，访问顺序为：A -> B -> D -> C -> E
后序遍历为例，访问顺序为：D -> B -> C -> A
中序遍历为例，访问顺序为：D -> A -> E -> C

BFS 
层序遍历 队列

## 列表转树 listToTree